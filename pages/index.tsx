import Layout from '@/components/Reusable/Layout'

/**
 * This page simply renders a blank page.
 * It should quickly redirect to either the dashboard or the signin page
 * depending on whether the user is logged in or not.
 */
export default function Index() {
    return (
        <Layout>
            <div />
        </Layout>
    )
}
